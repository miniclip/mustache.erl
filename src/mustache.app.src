{application, mustache, [
    {description, "Mustache Templates Rendering Library"},
    {vsn, git},
    {registered, []},

    {applications, [
        % OTP dependencies
        kernel,
        stdlib
        % External dependencies
    ]},

    {env, []}
]}.
